<header class="site-header">
  <div class="header-inner">
    <a class="brand" href="/" aria-label="Yuxuan Zhou home">
      <span>Yuxuan Zhou</span>
      <small>Design Engineering</small>
    </a>
    <nav aria-label="Primary navigation">
      <a href="/#projects">Projects</a>
      <a href="/#about">About</a>
      <a href="/#contact">Contact</a>
      <a class="cv-link" href="/cv/yuxuan-zhou-cv.pdf">CV</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 20;
    width: 100%;
    padding: 18px 0 0;
    pointer-events: none;
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    width: min(100% - 48px, 1320px);
    min-height: 64px;
    margin-inline: auto;
    padding: 10px 10px 10px 18px;
    border: 1px solid rgba(246, 242, 234, 0.1);
    border-radius: 999px;
    background: rgba(6, 6, 5, 0.68);
    backdrop-filter: blur(18px);
    box-shadow:
      0 18px 60px rgba(0, 0, 0, 0.32),
      inset 0 1px 0 rgba(255, 255, 255, 0.06);
    pointer-events: auto;
  }

  .brand {
    display: grid;
    gap: 2px;
    color: var(--text);
    font-size: 0.92rem;
    font-weight: 760;
    text-decoration: none;
  }

  .brand small {
    color: var(--soft);
    font-size: 0.68rem;
    font-weight: 600;
    text-transform: uppercase;
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 8px;
  }

  nav a {
    min-height: 36px;
    padding: 9px 14px;
    border: 1px solid transparent;
    border-radius: 999px;
    color: var(--muted);
    font-size: 0.86rem;
    text-decoration: none;
    transition:
      color 180ms ease,
      border-color 180ms ease,
      background 180ms ease;
  }

  nav a:hover,
  .cv-link {
    border-color: var(--line);
    background: rgba(255, 255, 255, 0.035);
    color: var(--text);
  }

  @media (max-width: 640px) {
    .site-header {
      position: static;
      padding-top: 12px;
    }

    .header-inner {
      width: min(100% - 24px, 1240px);
      align-items: flex-start;
      flex-direction: column;
      border-radius: 18px;
    }

    nav {
      justify-content: flex-start;
    }
  }
</style>
